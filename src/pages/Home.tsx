import { Catalog } from "../components/Catalog/Catalog";
import { Hero } from "../components/Hero/Hero";

export const Home = () => {
    return (
        <section>
            <Hero />
            <Catalog />
        </section>
    );
};
